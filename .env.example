VITE_API_URL=/api/sapp
VITE_DEV_PROXY_TARGET=http://localhost:8080
# Opcional: endpoint absoluto o relativo que invalida la cookie HttpOnly/sesion del Gateway.
# Por defecto se usa `${VITE_API_URL}/logout`.
# VITE_LOGOUT_URL=/api/sapp/logout
